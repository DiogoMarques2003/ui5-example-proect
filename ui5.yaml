specVersion: '4.0'
metadata:
  name: ui5.ui5proj
type: application
server:
  customMiddleware:
    - name: ui5-middleware-simpleproxy
      afterMiddleware: compression
      mountPath: /V2
      configuration:
        baseUri: 'https://services.odata.org'
